# Решения (ADR-лайт)

Зафиксировано по ответам заказчика 2026-09-13.

| # | Тема | Решение |
|---|---|---|
| 1 | Источник | `tt.chuvsu.ru`, гостевой вход. Личный кабинет не нужен, учётка студента не используется. |
| 2 | Аудитория | Только ВИШ (факультет 32). Заочные `ОЗВИШ-*` скрыты и не опрашиваются (`HIDDEN_GROUP_PREFIXES`). |
| 3 | Объединение групп | Одинаковые номер и год = одна группа: `ВИШ-12-23` + `ВИШ-12-23иот (…)`, `ВИШ-13-23` + `ВИШ-13-23иот`. Разные квалификаторы без «иот» остаются разными: `ВИШ-11-23 (ЭиЭА)` ≠ `ВИШ-11-23(РЗиАЭС)`. Следы «иот» пользователю не показываются, расписание = объединение вариантов. |
| 4 | Опрос | Каждые 6 минут в 07:00–21:00 пн–сб, каждые 30 минут в остальное время (cron в `.env`). Между запросами к порталу 0.9 с. У портала нет API, только HTML. |
| 5 | Что считать изменением | Диф по конкретным датам: добавлена, убрана, перенесена, сменилась аудитория/время/формат. Уведомляем о датах от сегодня до +14 дней; в «Изменениях» показываются только будущие изменения, прошедшие исчезают. |
| 6 | Напоминания | Пользователь выбирает сам: до первой пары (30 мин–3 ч), перед каждой парой, отдельно перед дистанционной парой (5/10/15 мин) со ссылкой на `tt.chuvsu.ru/webinar`, вечером на завтра, тихие часы. «Рекомендуемые в один клик» = изменения + за 2 часа до первой пары + за 5 минут до дистанта. |
| 7 | Подгруппы | Опциональный фильтр в настройках; по умолчанию показываем обе подгруппы. |
| 8 | Формат | Текст, картинка-постер или оба (по умолчанию оба). Постеры рисует satori + resvg без браузера. |
| 9 | Сессия | Расписание сессии подгружается, изменения по нему приходят только тем, кто включил «Сессия» в настройках. |
| 10 | Новости и объявления | Три темы: «Конкурсы и стипендии», «Объявления», «События ВИШ». Источники: ручная рассылка админа (с подтверждением), канал по хэштегам, ежедневный ИИ-сканер источников (`/sources`). Красный баннер портала уходит только админам и в «Изменениях» не показывается. |
| 11 | Медиа-ВИШ | Кнопка «Предложить новость»: сообщение пересылается в чаты из `MEDIA_CHAT_IDS`. |
| 12 | ИИ | Модель `claude-sonnet-5` везде. `/ask` работает через tool runner: инструменты «расписание любой группы», «найти предмет по разговорному названию», «список групп»; кнопка «Ответ неверный» шлёт вопрос-ответ админу. Лимиты: 10 вопросов/день на человека, 300/день всего. Включается наличием `ANTHROPIC_API_KEY`. |
| 13 | Админ | Telegram ID `5985343016` (список в `ADMIN_IDS`). Команды `/admin`, `/stats`, `/health`, `/poll`, `/broadcast`. |
| 14 | Тон | На «ты», сдержанно. |
| 15 | Стек | Node.js 22, TypeScript, grammY, chuvsu-js, SQLite через встроенный node:sqlite, croner. MariaDB хостинга не используется. |
| 16 | Хостинг | Pterodactyl (FrienWorld), egg Node.js, long polling. HTTPS не нужен. Автодеплой из GitHub Actions через Client API. |
| 17 | Секреты | Только в `.env` на сервере и в GitHub Secrets. В репозитории и переписке не хранятся; скомпрометированные токены заменить. |
| 18 | Inline-режим | `@бот 12-23 завтра` в любом чате вставляет расписание дня. |
| 19 | Преподаватели | Портал показывает расписание преподавателей только авторизованным. Раздел работает при заданных `PORTAL_LOGIN`/`PORTAL_PASSWORD` (отдельный клиент с учёткой), справочник кешируется на сутки. Поиск нечёткий: фамилия и имя в любом порядке, инициалы, префиксы; если локально пусто — поиск на портале. Последняя ошибка портала видна в `/health`. |
| 20 | Поток | Режим «Поток»: все группы одного набора (23/24/25/26) одним экраном, одинаковые пары объединяются, «Общие пары» = лекции, где своя группа сидит с другими. |
| 21 | Источник новостей | Каналы/чаты из `NEWS_CHANNEL_IDS` (бот там админ). Пост с хэштегом `#конкурс`, `#объявление`, `#событие` (и синонимами) уходит подписчикам темы, `#всем` — всем. Без хэштега пост игнорируется. Плюс ручная рассылка админа. |
| 22 | Клавиатура | 12 кнопок: Сегодня, Завтра, Др. группы / Неделя, Следующая, Поток / Изменения, Календарь, Преподаватели / Функции, Поиск, Настройки. «Отправить новость» осталась командой `/suggest` (без `MEDIA_CHAT_IDS` уходит админам). |
| 23 | ИИ-сканер новостей | Раз в день (`NEWS_SCAN_CRON`, 10:30 МСК) бот читает источники из `/sources`: превью публичных Telegram-каналов (`t.me/s/…`), стены VK (нужен `VK_SERVICE_TOKEN`), ленту Tilda сайта ВИШ. Посты за `NEWS_LOOKBACK_HOURS` (32 ч) Sonnet 5 раскладывает по темам, подходящие уходят подписчикам с кнопкой «Не по теме» (жалоба уходит админам). Не более `NEWS_MAX_PER_TOPIC` (8) постов на тему за скан. |
| 24 | Календарь | Два режима. Подписка: личная ссылка `PUBLIC_URL/cal/<token>.ics` (HTTP-сервер бота на порту `SERVER_PORT`), календарь телефона обновляет её сам, дубликатов нет. Файл .ics: пары до конца семестра, будильник за 15/30/60 мин; UID пары стабилен, `SEQUENCE` растёт с каждой генерацией, убранные/перенесённые пары идут как `STATUS:CANCELLED`. Под «Изменениями» и в уведомлении об изменениях — файл «только изменения». Будильник телефона напрямую включить нельзя: ближайшее — напоминание в событии календаря. |
| 25 | Рассылка | Перед отправкой показывается аудитория и число получателей, нужен явный «Отправить»; кнопка «Отмена» на каждом шаге. |
| 26 | Доска объявлений | Рассылка «Всем» или в тему «Объявления» попадает на доску в «Изменениях» на 24 часа (несколько сразу); админ может убрать раньше кнопкой под отчётом о рассылке или через `/announcements`. |
| 27 | Актуальность изменений | В «Изменениях» показываются только события с датой ≥ сегодня, созданные за последние 14 дней; прошедшие исчезают сами. |
| 28 | Слежение за группами | Кнопка «Убрать все слежения» в настройках; в уведомлении по чужой группе — «Не следить за группой». Уведомления по своей группе получают кнопку «Обновить в календаре». |
| 29 | Постеры | Стрелки обновляют картинку на месте (`editMessageMedia`), а не шлют новую. Чётность недели — отдельная плашка в шапке. Тема оформления выбирается `POSTER_THEME` (midnight по умолчанию; варианты editorial, brutalist, timeline). |
| 30 | Эмодзи | Без ярко-красных «тревожных» эмодзи (🚫 🆘 ❌ ⛔ 🛑); мягкие красные оттенки допустимы. |
| 31 | Преподаватели без учётки | В расписании групп портал преподавателей не отдаёт вообще (проверено: 3297 пар, 0 с преподавателем). Единственный гостевой источник — страница вебинаров `/webinar` (дата + факультет): там есть преподаватель с должностью и степенью, тема занятия и группы. Бот опрашивает её раз в час на 3 дня вперёд, хранит 60 дней и использует для: напоминаний о дистанте (преподаватель + тема), подстановки преподавателя и темы в расписание дня и недели, поиска преподавателей и ответов ИИ, когда учётки портала нет. С учёткой дополнительно работает полный справочник и очные пары. |
| 32 | Тихие часы и изменения | Уведомление, попавшее в тихие часы, не теряется: каждое доставленное изменение помечается по пользователю, а после окончания тихих часов бот присылает накопившееся одним сообщением. Для уже работающих установок точка отсчёта ставится при первом запуске, поэтому старые события не рассылаются повторно. |
| 33 | Поиск и ИИ | Кнопка «Поиск» ищет группы, предметы (своя группа, затем все) и преподавателей. `/ask` отвечает и про преподавателей инструментом `find_teacher` (предметы, группы, ближайшие пары из расписания); биографию бот не знает. |
| 34 | Опечатки в именах | Поиск людей (преподавателей и студентов) прощает опечатки: слова сравниваются с расстоянием Дамерау — Левенштейна (1 ошибка для слов 4–6 букв, 2 для длинных). Совпадение «только по опечатке» помечается отдельно: бот не открывает чужое расписание молча, а предлагает кнопки «может быть, кто-то из них». ИИ получает те же кандидатов и называет их словами, кнопки под ответом ставит бот. |
| 35 | Лимиты ИИ | `AI_DAILY_LIMIT_*` из `.env` — значение по умолчанию. Админ меняет лимиты кнопками в админке (они живут в `meta` и переживают перезапуск) и может разово поднять квоту на сегодня («буст», сам исчезает завтра). Админы отвечают без лимита. |
| 36 | Объявления | Тема «Объявления» включена по умолчанию (там только важное: дистант, отмены, дедлайны); конкурсы и события остаются по подписке. Миграция включает её и тем, у кого список тем был пуст — в том числе тем, кто когда-то снял все галочки: отличить «не открывал настройки» от «снял осознанно» в базе нечем, а важное должно доходить. Выключается там же, в ⚙️ Настройках. Доска объявлений в «Изменениях» по-прежнему наполняется только явным переключателем при рассылке. |
| 37 | Кнопка «сегодня» | Убрана из всех навигаций по дням (своя группа, чужая группа, поток, преподаватели): между стрелками её читают как текущую дату. |
| 37.1 | Совпадения «с натяжкой» | Нечёткое совпадение (опечатка или более длинная форма фамилии) никогда не открывает чужое расписание молча: и у преподавателей, и у студентов бот показывает кнопки «может быть, кто-то из них». То же правило у групп: если строка реестра подходит сразу к двум разным группам портала (ВИШ-11-23 бывает ЭиЭА и РЗиАЭС), бот спрашивает, а не выбирает первую. |
| 38 | Глобальный поиск студентов («сыск») | Включается `POISK=TRUE`. Данные о людях лежат в отдельном файле на хостинге (`POISK_DB`), в репозиторий не попадают; бот читает из него только ФИО, группу и подгруппу. Показывает предполагаемое место по расписанию группы (честно предупреждая, что это расписание, а не факт присутствия) и день с обычными кнопками. Каждый поиск пишется в `poisk_log` (аудит + лимит в сутки), журнал переживает `/soon` и чистится через полгода. При `POISK=FALSE` Composer не подключается вообще. |
| 39 | Inline-режим | Telegram сообщает в `getMe`, включён ли inline. Бот обещает «@бот 12-23» в «Функциях» только когда он действительно включён, а в `/health` показывает состояние и что делать (@BotFather → `/setinline`). |
| 40 | Слова-даты | «послезавтра», «позавчера» и любые цепочки («послепослезавтра» = сегодня + 3) понимаются и в личке, и в inline: каждое «после» двигает день вперёд, каждое «поза» — назад. |
| 41 | Inline | Через inline доступны все основные экраны: день, неделя, поток и общие пары, с любой группой и датой. Список результатов собирается под запрос, id режется по 64 байтам (кириллица — два байта на букву). |
| 42 | Карта преподавателей | Отдельная таблица `teacher_map`: кто ведёт у ВИШ, какие группы и предметы, кафедра, фото. Наполняется двумя источниками — страницей вебинаров (всегда) и фоновым обходом справочника портала по 40 человек за заход (только с учёткой). Пометка «(ВИШ)» рядом с фамилией решает проблему полных тёзок; единожды выставленная, она не снимается пустым обходом. |
| 43 | Фото преподавателя | Приходит отдельным сообщением ПОСЛЕ расписания: вверх никто не листает. Telegram file_id запоминается, чтобы не качать одно и то же фото с портала. |
| 44 | Слежение за преподавателем | Кнопка в его расписании. Вечером — его завтрашний день, за 2 часа до первой пары — сегодняшний. Портал опрашивается один раз на преподавателя, а не на каждого подписчика; день кешируется на 3 часа. |
| 45 | Время конца пары | Показывается везде: в дне, в неделе, в потоке и на всех четырёх темах постеров (в недельных — отдельной строкой под началом). |
| 46 | Файл изменений | Кнопка «📆 Файл изменений в календарь» теперь в каждом уведомлении об изменении, включая чужие группы и утреннюю выдачу после тихих часов. |
| 47 | Запись вебинаров | Отдельный сервис `recorder/` на своём контейнере: заходит в комнату BigBlueButton по ссылке от `POST /webinar/getjoin`, снимает слайды (кадр сохраняется, только когда слайд устоялся) и отдаёт боту PDF на `POST /slides` с общим токеном. Бот рассылает его тем, у кого эта пара в расписании. Бот виден в списке участников — это осознанно: скрытая запись чужой лекции была бы нечестной. |
| 48 | Доставка изменений | Пометка «доставлено» ставится только после успешной отправки, а утренний добор после тихих часов смотрит и свою группу, и те, за которыми следят. Человеку без тихих часов добор служит повтором недавней неудачной отправки (события не старше двух часов), а не пересказом дня. |
| 49 | Длина callback_data | Ключ группы приходит из названия на портале и бывает длинным, а Telegram отвергает всю клавиатуру при callback_data > 64 байт. Кнопки собираются через `groupCb()`, который режет ключ по байтам, а `ScheduleService.group()` принимает такой обрезок, если он однозначен. |
| 50 | Секреты в ошибках | Текст ошибки сети уходит в базу, логи и админу, поэтому адрес в нём — только origin и путь: в запросе к VK лежит сервисный токен. |
| 51 | Правки по ревью (раунд 8) | Журнал «сыска» и прочие окна по времени сравниваются в одном ISO-формате (раньше «две минуты» превращались в сутки). Новости из обычного чата пересылаются только от админов чата. Пост, который ИИ не разложил по темам, разбирается следующим сканом (до 72 ч). `/ask` и сканер: `max_tokens` с запасом под размышление модели, обрезанный ответ помечается. Inline-запрос и реплика в группе не заводят пользователя. 403 «bot can't initiate conversation» помечает человека недоступным во всех рассылках. |
| 52 | Единая карточка человека | Преподаватели и студенты ищутся одним поиском (кнопка «Преподаватели», «Поиск», ИИ, inline) и выглядят одинаково: кто это, «где сейчас» по расписанию (теперь и у преподавателя), пары дня. Кнопки одни и те же: ◀️ ▶️ по дням, «Неделя», «Найти другого»; у преподавателя — «Следить», у студента — «Вся группа». Колбэки `pp:`/`ppw:`/`ppo:`/`ppg:`/`ppf:`, старые `t:`/`td:`/`tw:`/`pos:` работают. Студенты — только при `POISK=TRUE`, с лимитом и журналом. Кнопки «следить за студентом» нет сознательно: это была бы слежка за человеком. |
| 53 | Inline по фамилии | `@бот Беляев завтра` — расписание человека на день, кто бы он ни был. Студенты — только при `POISK=TRUE`, от 4 букв, каждый ответ пишется в журнал и тратит лимит. Нечёткое совпадение не открывается молча — приходят варианты. |
| 54 | ЛК / ПР / ЛБ | Типы пар везде короткие. Преподаватель стоит в строке типа: `ЛК · ауд. Г-101 · Иванова И. И.`, фамилия с инициалами неразрывна и не переносится (текст и все темы постеров). |
| 55 | Режим преподавателя | `/prepod`, реестр `TEACHERS_DB` («ФИО;ник», только на сервере). Преподаватель со своего аккаунта получает то же меню, что студент, но «своя группа» — это он сам: день, неделя, напоминания, календарь, `/ask`, `/subjects` — по его парам; третья кнопка — «👥 Студенты» (то же, что «Др. группы»). Кнопки в режим нет, команда — только в меню админов; админ может проверить режим на любом: `/prepod ФИО`. |
| 56 | /start одним сообщением | Приветствие и одна кнопка «💬 Спросить?» (ИИ-поиск). Лимит у неё общий с `/ask` и «Поиском» — «🤖 Лимиты «Спросить»» в админке. Новому человеку в том же сообщении — выбор группы, клавиатура меню приходит вместе с подтверждением группы; вернувшийся видит уже стоящее меню. |
| 57 | Панель предметов (тест) | `/subjects` — только команда, без кнопки: предметы семестра кнопками, у предмета — таблица всех пар (дата, день, время, ЛК/ПР/ЛБ с подгруппой, аудитория; ✓ прошла, → ближайшая), итоги по типам, фильтр по типу. `/subjects 14-24` — другая группа, в режиме преподавателя — свои пары. |
| 58 | Болталка в группах | Включена по умолчанию (`CHAT_AI`, нужен ключ Anthropic): «@бот …» или ответ на сообщение бота — ответ через Claude (модель `CHAT_AI_MODEL`, по умолчанию как `AI_MODEL`; свой ключ `CHAT_ANTHROPIC_API_KEY` или общий). Контекст: прошлые обмены с этим человеком в этом чате (`chat_log`, 3 дня, стирается `/soon`), недавняя переписка чата (только в памяти), сообщение, на которое ответили. Про пары, преподавателей и бота отвечает инструментами `/ask`, но без поиска студентов: о конкретных людях в общем чате — ни слова. Где болтать, решает админ бота: чат, куда бота добавил он сам, включается автоматически; добавил другой — админам приходит вопрос с кнопкой «Разрешить»; ещё `/chaton`/`/chatoff` в чате, кнопки в `/chats`, `CHAT_GROUP_IDS`. Пересланное сообщение с @ботом — не обращение; пауза 3 с между ответами в чате. Лимиты на день — на человека, на чат и общий, меняются в админке (`meta`), разовая добавка на сегодня; админы без лимита. Отказ модели и `max_tokens` обработаны; о лимите бот говорит один раз в день. В логах только цифры. |
| 59 | Сценарий болталки | Ответ всегда пишет ИИ, база «вопрос;ответ;подсказка» (`CHAT_QA_DB`, CSV) — его заготовки, и они главнее идей модели. Вся база лежит в системном промпте с кешем (модель узнаёт вопрос и другими словами), совпавшие по словам или с опечаткой заготовки дополнительно подсвечиваются. Файл перечитывается сам; из бота — `/qa`, `/qa_add`, `/qa_test`, `/qa_file`, `/qa_import` (старая версия — `.bak`, пустой файл не принимается, cp1251 от Excel читается). |
| 60 | Privacy mode | По документации Telegram, бот в privacy mode получает в группе только команды, ответы на свои сообщения и сообщения через себя; простое «@бот привет» не приходит. Для болталки privacy mode выключается в @BotFather (`/setprivacy` → Disable) с повторным добавлением бота в группу, либо бот делается админом группы. Состояние видно в `/health` и в «💬 Болталка». С выключенным privacy mode бот видит все команды чата, поэтому отвечает «пиши в личку» только на адресованные ему (`/cmd@бот`). |
| 60.1 | Две версии болталки | Параллельно были сделаны две версии бота в группах; объединены в одну (эта). Из первой взяты ответы по расписанию инструментами, игнор пересылок, пауза, кнопка «Разрешить», `/chaton`/`/chatoff` и имена команд (`/chats`, `/replies`, `/reply_add`, `/reply_del` работают). Не взято мгновенное «точное совпадение — ответ без ИИ»: по решению заказчика отвечает всегда ИИ, заготовка — сценарий. Миграция первой версии (`group_chats`, `canned_replies`) остаётся на своём месте, новые идут после неё и переносят чаты и лимит; ответы из `canned_replies` один раз переносятся в файл сценария. |
| 61 | Справочник преподавателей | Список преподавателей портал отдаёт на `/index/techfac` (старый `/index/tech` больше не работает). Бот берёт его оттуда; если страница просит факультет — пробует её с `FACULTY_ID`; старый адрес остаётся запасным. Разбор понимает кнопки `.techbut`, `name="tech<id>"` и ссылки на `/index/techtt/tech/<id>`. `/health` различает «учётка не вошла (ответ портала), бот ходит гостем» и «учётка вошла, но справочник пуст (что на какой странице)». |
| 62 | Кнопка «🔍 ИИ поисковик» | Бывшая «🔍 Поиск» — это ИИ-поиск, подпись теперь это говорит. Старая подпись остаётся рабочей: нижнее меню живёт в чате, пока его не заменит новое. |
| 63 | Преподаватель — «своя группа» сам | Аккаунт из реестра преподавателей на `/start` сразу попадает в режим преподавателя: «Здравствуйте, Имя Отчество! Рад вас видеть», его расписание на сегодня, предложение включить напоминания — без выбора группы. Полные тёзки в справочнике — кнопки «кто из них вы». Кто сам выключил режим `/prepod` (в том числе до появления метки: «ФИО есть, режима нет»), того `/start` обратно не включает (метка в `meta`, стирается по `/soon`); `/prepod` включает снова. «Усиленная анонимность» — не узнаём. Не нашёлся в справочнике при первом `/start` — бот ищет снова при каждом открытии своих пар. Изменения по старой студенческой группе в режиме преподавателя не приходят (за группами можно следить отдельно). |
| 64 | Пасхалка | Кирилл Ершов, спорторг ВИШ: с его подачи в боте появились преподаватели, а в тот день бот разослал всем «изменения во всех предметах» (у каждой пары впервые появился преподаватель). Карточка по запросу «Ершов» в ИИ-поисковике, `/ask`, поиске преподавателей и inline; секретная `/ershov` с «достижением». Однофамильцев из расписания пасхалка не прячет. |
| 65 | Свои преподаватели первыми | При равном совпадении преподаватель ВИШ идёт впереди однофамильцев со всего университета (раньше решал алфавит, и «Петров К.» из ВИШ мог не попасть в выдачу). Преподаватели, помеченные «(ВИШ)» в карте, ищутся, даже если их нет в суточном справочнике. Если одинаково подходят несколько преподавателей, а из ВИШ ровно один, — сразу его карточка, а остальные такие же — кнопками под ней. |
| 66 | Карточка человека картинкой | В формате «картинка»/«оба» расписание преподавателя или студента приходит постером той же темы, что и у групп; подпись — кто это и где он сейчас. На постере преподавателя вместо фамилии (это он сам) — группы: рендерер получает `labels: "groups"` и подставляет их сам, данные расписания не трогаются. Так же выглядят свои пары и напоминания в режиме преподавателя. Студент без группы в расписании — текстом, а не пустым постером. Листание по текстовой карточке остаётся текстом, как у групп. |
| 67 | Записывалка: не терять пары | Неудачная попытка (портал не дал ссылку на комнату, страница не открылась, ни одного кадра, запись упала) повторяется раз в минуту до конца пары — раньше после трёх осечек за первые минуты пара терялась. Закрытую ведущим комнату бот замечает на каждом круге и больше туда не ходит. Параллельные пары пишутся одновременно, до `MAX_PARALLEL` (по умолчанию 2, каждая — свой Chromium); не поместившиеся ждут места, в лог — один раз. Ожидание кнопок входа — свой цикл до 30 с (`isVisible` у Playwright не ждёт). Область презентации ищется заново, когда пропала или кадр не снялся. Появившийся номер слайда у того же кадра — не смена слайда. Кадры и PDF — по дате, времени начала и предмету; папка кадров чистится перед съёмкой. Непринятый ботом PDF остаётся с меткой `.pending.json` и уходит повторно раз в 5 минут. Пустая переменная в `.env` — значение по умолчанию. Разведка не подставляет чужую пару, если нужные закрыты или не проходят фильтр. |
| 68 | Правки по ревью | Пасхалка: однофамильцы после карточки — одна функция `ershovNamesakes` на все места (поиск, ИИ-поисковик, `/ask`, inline): ищет по фамилии, а не по всему запросу (иначе «Кирилл Ершов» давал любого Кирилла), только точную фамилию Ершов/Ершова (не «Ежов» с опечаткой), только среди преподавателей своей базы (студентов не ищет и в журнал поиска людей не пишет, на портал не ходит), на «спорторг» — никого; однофамильцы только кнопками, сами не открываются. `/ask` идёт к ИИ после карточки, только если такой однофамилец есть. Выбор себя из тёзок (`/prepod` и `/start`) держит ФИО в метке `tmode:fio` по сообщению с кнопками, а не в `teacher_name`: незаконченный выбор не выглядит «выключил сам», а у админа два выбора подряд не путают ФИО. Старые записи «ФИО есть, режима нет» считаются отказом, только если есть и преподаватель из справочника; без него это брошенный выбор, и `/start` встретит. Тёзки под карточкой помнятся по человеку (час с последнего обращения, до 20 карточек, протухшее выметается): стрелки старой карточки их не теряют, у открытого тёзки — кнопки к первому и остальным. `groupByDate` — одна функция в `schedule/model.ts` вместо копий. |
| 69 | Ревью всего проекта | Весь `src/` проверен целиком шестью ревьюерами (раньше `/code-review` смотрел только последний коммит). Исправлено: кривой адрес HTTP-запроса («GET //[») ронял весь процесс — теперь 400; токен бота из сетевых ошибок grammY попадал в лог — логгер вычищает токен и ключ Anthropic; первая калибровка недели нового семестра больше не рассылает «➕» по всем парам (база перестраивается без сравнения); календарь держит всю зимнюю сессию и не пустеет после конца окна; перенос с видимой целью — одно событие; `/soon` стирает подписки на преподавателей; утренний добор после тихих часов не вываливает старые изменения только что добавленной группы; объявления портала в тихие часы — без звука; журнал отправок чистится (месяц) и индексирован (миграция); лимит `/ask` не пробивается параллельными вопросами; `/chatoff` не отменяется упоминанием админом (метка `chat:off:<id>`) и честно отвечает для чатов из `CHAT_GROUP_IDS`; последний круг инструментов болталки — без инструментов; кнопка «💬 Болталка» в `/admin` работает; повторное «🔕» не подписывает на преподавателя снова; старый экран подтверждения рассылки не отправит новое сообщение; `/cancel` не становится текстом рассылки; пустое число в `.env` — значение по умолчанию, а не 0; пустой `LOG_LEVEL` не роняет старт; «31.09» — не дата; «пары завтра» в inline — день, а не поиск человека; кнопки чужой группы укладываются в 64 байта; «Заблокировал бота» — только по 403 из лички; кнопка поиска ищет студентов только по запросу, похожему на имя; портал: учётка перепробуется раз в полчаса и в живой гостевой сессии, обход справочника гостем не идёт, главная или техработы вместо страницы группы/преподавателя/вебинаров — ошибка, а не «пар нет», фото не сбрасывает сессию на 404, кука не уходит на чужой хост, обрезанные картинки не отдаются, справочник после неудачи ждёт 15 минут; реестры в cp1251 читаются; «И. Иванова» находится точно, двойная фамилия — не другой человек; пометка «(ВИШ)» с вебинаров находит преподавателя по полному ФИО; группы с разным профилем в скобках не путаются; новости: хэштеги с русскими окончаниями и границей слова, страница сайта не приходит «новой» каждый день, дубли из привязанного канала и повтор id классификатора не шлются дважды, анонимный админ группы-источника может постить «#всем»; сценарий болталки: разделитель — из текущего файла, удаление не пересобирает файл с неразобранными строками; воскресенье с парами на недельном постере midnight; длинное ФИО на постере brutalist укорачивается. |

## Открытые вопросы

- Mini App (веб-интерфейс недели) требует HTTPS-домена; на хостинге есть слот «proxies: 1», нужно проверить, даёт ли он домен с SSL для порта 40070.
- Образ Node 18 в egg'е: попросить FrienWorld добавить `nodejs_22`; до этого работает лаунчер с портативным Node.
- Перенос выходных по производственному календарю (1 и 9 мая на выходные и т. п.) бот не знает: праздники — фиксированные даты. Нужен список нерабочих дней на год.
- Альбом из канала с хэштегом уходит подписчикам одной фотографией (с подписью): остальные фото альбома приходят без хэштега.
- Одна «ядовитая» новость (отказ модели, нечитаемый ответ) валит свою пачку из 30 при каждом скане; соседи догоняются, пока не выйдут 72 часа.
