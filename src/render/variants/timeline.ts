import { createRenderer as base, type Renderer } from "../image.js";

/** Placeholder: replaced by the "timeline" design. */
export async function createRenderer(): Promise<Renderer | null> {
  return base();
}
